import { fromToToCoordinates } from "./from-to-to-coordinates";
import { fromToToPosition } from "./from-to-to-position";
import { getDefaultArg } from "./get-default-arg";
import { multipyByDefaultArg } from "./multiply-by-default-arg";
import { recursiveModel } from "./recursive-model";

const McUtils = {
  fromToToCoordinates,
  fromToToPosition,
  recursiveModel,
  multipyByDefaultArg,
  getDefaultArg,
};

export { McUtils };
